# Serve client.json when /client is requested
RewriteEngine On
RewriteRule ^client$ client.json [L]

# Serve server.json when /server is requested
RewriteRule ^server$ server.json [L]

# Set content type for JSON files
<FilesMatch "\.json$">
    ForceType application/json
</FilesMatch>
